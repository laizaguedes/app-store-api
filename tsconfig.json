{
  // Visit https://aka.ms/tsconfig to read more about this file
  "compilerOptions": {
    "target": "es5", /* versao do javascript */
    "module": "commonjs", /* sistema de modulos, forma como o javascript/typescript organiza e compartilha código entre arquivos */
    "strict": true, /* ativa o modo estrito do typescript, ou seja, várias validações extras de tipo */
    "esModuleInterop": true, /* permite importar módulos commonJs usando a sintaxe de ESM (import ... from ...) */
    "skipLibCheck": true, /* faz o compilador ignorar a checagem de tipos nos arquivos .d.ts (arquivos de definição de bibliotecas) */
    "forceConsistentCasingInFileNames": true /* padroniza letras maiusculas/minusculas em nomes de arquivos, exemplo: ./User.ts e ./user.ts. */
  }
}